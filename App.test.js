import React from 'react';
import renderer from 'react-test-renderer';

import App from './App';

describe('<App />', () => {
    it('<App /> doesnt test anything', () => {
        expect(1).toBe(1);
    });
});