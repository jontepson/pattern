import fetch from 'node-fetch'
import React from 'react';
import renderer from 'react-test-renderer';
import {
    fetchBikesApi,
    fetchCitiesApi,
    fetchUserApi
} from '..'


describe('Test mocking fetch functions', () => {
    it('Doing nothing atm', () => {
        expect(1).toBe(1);
    });
});